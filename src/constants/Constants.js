const PizzaSizes = {
  SMALL: "small",
  MEDIUM: "medium",
  LARGE: "large"
};

const PersonTypes = {
  ADULT: "adult",
  CHILDREN: "children"
};

const OrderConfig = {
  MIN_ORDER_VALUE: 200,
  MAX_ORDER_VALUE: 1000
};

export { PizzaSizes, PersonTypes, OrderConfig };
